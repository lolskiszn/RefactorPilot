# Complex Service Example

This example exists to exercise the next transformer tier:

- early returns with `http.Error`
- middleware chain
- path parameters
- query extraction

Try:

```bash
node ./src/cli/index.js preview ./examples/complex-service --pattern rest-to-grpc-full
```
